## Setup env variable
```
export SSLKEYLOGFILE=$HOME/Desktop/keylogfile.txt
firefox
```
## Enable KEY logging in wireshark

In Wireshark, go to Edit -> Preferences -> Protocols -> TLS, and change the (Pre)-Master-Secret log filename preferenc
